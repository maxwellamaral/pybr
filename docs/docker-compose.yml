services:
  jekyll:
    image: jekyll/jekyll:latest
    container_name: pybr-jekyll
    command: jekyll serve --force_polling --livereload
    ports:
      - "4000:4000"
      - "35729:35729"
    volumes:
      - .:/srv/jekyll
    environment:
      - JEKYLL_ENV=development
